# Found Calc — Phase Handoff

**Project:** Found Calc
**Last canonical completed phase:** Phase 01 — Repository & Cloudflare Foundation
**Current status:** COMPLETE
**Next phase:** Phase 02 — Deterministic Engine + Reference Vertical Slices

## Rule: one phase = one new chat

Start each implementation phase in a **new chat inside the same Found Calc project**. Attach the latest canonical phase ZIP. For Phase 02, attach:

`found-calc-phase-01-foundation.zip`

The ZIP is the portable recovery/handoff baseline. GitHub is the collaborative source repository; the two are complementary.

## Required reading order for Phase 02

1. `BASELINE.md`
2. `PHASE_HANDOFF.md`
3. `docs/verification/phase-01-verification.md`
4. Master Product & Architecture Design Spec
5. Tech Stack ADR
6. Design System Decision
7. Design System Requirements
8. Component Inventory
9. Accessibility & Responsive Contract
10. Phase Workflow
11. the Phase 02 implementation plan created through Superpowers before coding

## Starter prompt for the new Phase 02 chat

```text
@Superpowers

Start Found Calc Phase 02 — Deterministic Engine + Reference Vertical Slices from the attached canonical `found-calc-phase-01-foundation.zip`.

Before implementation:
1. Read BASELINE.md and PHASE_HANDOFF.md.
2. Read docs/verification/phase-01-verification.md.
3. Read the approved Master Product & Architecture Design Spec, Tech Stack ADR, Design System Decision, accessibility contract, and phase workflow.
4. Treat Phase 01 architecture and repository boundaries as approved baseline; reopen them only for a verified implementation blocker under change control.
5. Use Context7 for current framework/library documentation and the Cloudflare skill/current Cloudflare docs for Workers-specific work.
6. Use Superpowers brainstorming/writing-plans/TDD/systematic-debugging/verification-before-completion as required.
7. Keep deterministic engine truth pure and separated from rule/catalog/UI concerns.
8. Preserve ID/EN, accessibility, privacy/trust constraints, and Rp0 fixed-infrastructure target excluding domain/payment transaction fees.

Create the Phase 02 implementation plan first. Do not pull Phase 03+ scope forward.
At Phase 02 completion, produce the next complete ZIP baseline and updated handoff.
```

## Phase 01 completion evidence

Phase 01 is complete because the repository foundation now has:

- committed dependency lockfile generated by a real network-enabled pnpm install;
- frozen-lockfile CI install;
- passing foundation tests, lint, and TypeScript checks;
- passing unit Vitest;
- passing Cloudflare Workers Vitest with D1 `DB` binding;
- passing Playwright ID/EN and route-boundary smoke tests;
- passing canonical `next build`;
- passing `vinext check` and `vinext build`;
- passing local built-Worker HTTP smoke for `/id` and `/en`;
- documented limitations and local-only D1 configuration;
- regenerated and clean-extraction-verified checksum manifest.

See `docs/verification/phase-01-verification.md` for the technical record.

## Phase 02 scope boundary

Phase 02 should implement deterministic engine foundations and representative reference vertical slices while preserving the package boundaries prepared in Phase 01. Authentication, billing, and other later-phase concerns must not be pulled forward unless the approved phase workflow explicitly requires them.

## Change control

If Phase 02 exposes a conflict with the approved specification or ADR:

1. stop the affected work;
2. explain the conflict and evidence;
3. propose the smallest amendment;
4. obtain approval before changing architecture.
