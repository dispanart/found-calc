# @found-calc/ui

Owns reusable Found Calc product primitives and contracts that are genuinely shared across calculator, workspace, and admin surfaces. Source-owned shadcn/ui primitives begin inside `apps/web` until reuse is proven.

**First implementation phase:** introduced only when cross-surface reuse is demonstrated in a later approved phase.

Phase 01 intentionally contains no shared runtime UI package code.
